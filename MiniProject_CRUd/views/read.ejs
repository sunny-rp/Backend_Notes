<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User List</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen text-white py-10 px-4">
  <div class="max-w-6xl mx-auto">
    <h1 class="text-3xl font-bold text-center mb-4">User List</h1>

    <!-- 🔙 Back to Home Link -->
    <div class="text-center mb-8">
      <a href="/" class="text-blue-400 hover:underline text-lg">
        ← Back to Home
      </a>
    </div>

   
    <% if (users.length === 0) { %>
      <h2 class="text-xl text-center text-gray-400">No users yet</h2>
    <% } else { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        <% users.forEach(user => { %>
          <div class="bg-gray-800 rounded-lg shadow-md overflow-hidden">
            <img src="<%= user.image %>" alt="User Image" class="w-full h-48 object-cover"/>

            <div class="p-4">
              <h2 class="text-xl font-semibold mb-1"><%= user.name %></h2>
              <p class="text-gray-300 mb-4"><%= user.email %></p>

              <div class="flex justify-between">
                <a href="/edit/<%= user._id %>"
                   class="bg-yellow-500 text-black px-4 py-2 rounded hover:bg-yellow-400 transition">
                  Edit
                </a>
                <form action="/delete/<%= user._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this user?')">
                  <a href="/delete/<%= user._id %>"
                          class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-500 transition">
                    Delete
                </a>
                </form>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
